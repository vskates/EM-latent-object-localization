# Assignment 3

В `REPORT.pdf` собран полный отчёт по задаче:

- модель;
- вывод формул для E-шагa, M-шагa и нижней оценки;
- описание реализации `EM`, `hard EM` и `run_EM_with_restarts`;
- генерация синтетических данных;
- анализ влияния инициализации, размера выборки, шума;
- сравнение `EM` и `hard EM`;
- пример запуска на реальных данных;
- результаты проверки `python3 run_tests.py`.

В коде также добавлена регрессионная проверка hard EM: если несколько изображений имеют один и тот же MAP-сдвиг, матрица `A` всё равно корректно суммируется к `1`.

Основной сдаваемый файл: `REPORT.pdf`.
